const initialState = {
    logo: '../src/logo.svg',
    code: 'src/App.js',
    appLink: 'https://reactjs.org'
}

const rootReducer = (state = initialState, action) => {
    return state;
}

export default rootReducer;